/* The phone shell — placeholder.

   The real shell stands in for the desktop shell inside a phone's browser: it
   defines window.__TAURI__, holds the connection to the Shop Hub, and answers
   the same commands the installed app answers. It is written next. */
